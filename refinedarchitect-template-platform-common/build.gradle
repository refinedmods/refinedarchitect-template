plugins {
    id("refinedarchitect.common")
}

refinedarchitect {
    common()
    publishing {
        // maven = false
    }
}

base {
    archivesName.set("refinedarchitect-template-platform-common")
}
